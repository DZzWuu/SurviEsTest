public struct PlayertHealthSignal
{
    public int Health;
    public int MaxHealth;

    public PlayertHealthSignal(int health, int maxHealth)
    {
        Health = health;
        MaxHealth = maxHealth;
    }
}
